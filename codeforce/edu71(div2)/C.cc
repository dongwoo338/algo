#include<bits/stdc++.h>
using namespace std;
typedef long long ll;
const int MAX = 2e9;
int main(){
    ios::sync_with_stdio(0);cin.tie(0);cout.tie(0);
    int t,n,a,b;
    string s;
    cin >> t;
    while(t--){
        ll ans= 0;
        cin >> n >> a >> b;
        cin >> s;

    }
}